using UnityEngine;

public class demo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("goedemiddag");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("goedenavond");
    }
}
